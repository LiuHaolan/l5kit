COLLISION_REWARD = -20

SAMPLED_GOAL_NUM = 4

"""
import pickle

cached = False
class AnchorConfig:


    def res():
        global cached
        if cached:
            init()
            cached = True
            return self.traj_anchor, self.yaw_anchor
        else:
            return self.traj_anchor, self.yaw_anchor
    
    @staticmethod
    def init():
        f = open('/mnt/home/v_liuhaolan/haolan/l5kit/nmp/anchor.pkl', 'rb')
        picklelist = pickle.load(f)
        self.traj_anchor = pickle.loads(picklelist[0])
        self.yaw_anchor = pickle.loads(picklelist[1])

"""

